import { type ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    // Seed Token，影响范围大
    colorPrimary: '#007498',
    // borderRadius: 2,

    // 派生变量，影响范围小
    // colorBgContainer: '#f6ffed',
  },
};

export default theme;